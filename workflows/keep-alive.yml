name: Keep Alive

on:
  schedule:
    # Каждые 14 минут
    - cron: "*/14 * * * *"
  workflow_dispatch: # Ручной запуск для проверки

jobs:
  ping:
    runs-on: ubuntu-latest
    steps:
      - name: Ping Render
        run: curl -fsS https://kanban-bot-pr1v.onrender.com/health || echo "Service unavailable"
